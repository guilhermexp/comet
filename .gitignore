/target
**/*.rs.bk
# Local-only scratch space — holds signing/notary credentials, never commit.
/ignore/
# Evidência de verificação local (runs isolados, logs, sandbox) — nunca commitada.
/.tmp/
*.pdb
.DS_Store
.env
.env.local
node_modules
.wrangler
build/
edge/dist/
.auto-code/
.reverse-engineering/
.plan-worktree-cutover.json
progress.md
