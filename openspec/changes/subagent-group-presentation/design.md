## Decisions
Use the same recorded task ID prefix relation already used by subagent batches. Only bound spawn calls can replace a parent row. Preserve failures and tasks with no bound children. Add the composer’s `theme.composer_glass_bg()` background (also on hover), radius 6px and 8px horizontal / 4px vertical padding to each agent link, sized to its content; the batch row has no fill; preserve per-agent links and status.

Keep batch links side by side without wrapping; long names truncate within shrinking links. Use an 8px gap between links.
