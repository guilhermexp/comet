Reuse the existing shell segment scanner for a bounded header summary. Short single commands remain readable; long or compound commands show up to four executable names. Heredoc summaries stop at the first line to avoid treating program text as commands. The existing call_block retains verbatim invocation. Expanded Exec places its existing header and scroll viewport in one frame, keeping the analytic height budget. Reasoning gets a left rule and inset without changing disclosure state or Markdown semantics.

Testable seam: command display projection versus verbatim call_block (quoted separators, Unicode, long paths, heredocs and active labels). Native typography, border and layout require visual inspection. Canonical gates: cargo test -p zeron-ui --lib, cargo build, cargo fmt --all.
