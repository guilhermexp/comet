Reuse the existing TurnSteps/final-answer split. Paint the separator once after the disclosure children, outside its open-only branch. Do not create another transcript row or classify live commentary as a final answer. Existing projection tests cover the split; the new hairline is visual-only and requires native acceptance rather than a source/style assertion. Verify with cargo test -p zeron-ui --lib, cargo build, cargo fmt --all --check.
