## Why
Generic tool rows repeat an unhelpful Ran tool/Running tool prefix before names such as ask.

## What Changes
Show the existing icon and tool name alone for generic Unknown tools, keeping the name as the primary neutral text. Preserve specific command/read/eval/hub labels, lifecycle indicators, errors and expanded payloads.

## Capabilities
### Modified Capabilities
- `turn-step-tool-groups`: generic tool header copy.

## Impact
Native UI presentation only.
