Use stream_copy, the existing native header adapter. Generic Unknown presentations omit the generic verb and retain the name in the shrinking detail slot; standalone names receive the primary header tone. No wire or shared export changes. Testable seam: projection across pending/completed states with semantic labels preserved. Verify cargo test -p zeron-ui --lib, cargo build and cargo fmt --all.
