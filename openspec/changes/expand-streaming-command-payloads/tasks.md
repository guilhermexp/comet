## 1. Implementation
- [x] 1.1 Make `detail_auto_open` follow the entry's streaming status and drop the resolved/last clause from `tool_detail_default_open`.
- [x] 1.2 Give `RowKind::FileChange` a streaming `auto_open` and keep the truncation notice honest.
- [x] 1.3 Update the transcript disclosure tests to assert the streaming/settled split.
- [x] 1.4 DOX pass on `crates/ui/AGENTS.md` and validate the change.
- [ ] 1.5 Review the native presentation with `scripts/dev-demo.sh` and archive after verification.
